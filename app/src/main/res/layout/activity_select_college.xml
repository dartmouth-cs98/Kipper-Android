<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:background="#ffffff"
    android:orientation="vertical"
    tools:context="com.artfara.apps.kipper.SelectCollegeActivity">

    <android.support.v7.widget.Toolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="?attr/colorPrimary">

        <TextView
            android:id="@+id/toolbar_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="left"
            android:text="Kipper"
            android:textColor="@color/colorPrimary"
            android:textSize="29sp" />
    </android.support.v7.widget.Toolbar>

    <TextView
        android:id="@+id/chooseSchoolMsg"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/toolbar"
        android:layout_gravity="center_vertical"
        android:text="@string/choose_you_re_school"
        android:textColor="@color/darkGray"
        android:textSize="30dp"
        android:padding="10dp"/>

    <ListView
        android:id="@+id/ListViewColleges"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:layout_below="@+id/chooseSchoolMsg"
        android:layout_above="@+id/wrapper_post"
        android:fadingEdge="vertical" />


</RelativeLayout>
